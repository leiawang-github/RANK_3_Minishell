#include "minishell.h"

//returning value of parser: a linked list




//count how many nodes in the list
int count_node(t_list simple_cmd)
{





}


// if (cmd->kind == CMD_ASSIGN_ONLY) { ... }
// else if (cmd->kind == CMD_REDIR_ONLY) { ... }
// else if (cmd->kind == CMD_ASSIGN_REDIR) { ... }
// else if (cmd->kind == CMD_WITH_ARGV) { ... }
